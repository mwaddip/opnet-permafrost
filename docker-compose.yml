services:
  permafrost-vault:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - permafrost-data:/data
    environment:
      - NODE_ENV=production

volumes:
  permafrost-data:
